export default class Estructura extends Phaser.GameObjects.Sprite {
    constructor() {
      super({ key: 'main' });


      this.stats = {
        position,
        owner,
        hp
      }

    }
    
}