export default class Casilla extends Phaser.GameObject.Sprite {
    constructor() {
      super({ key: 'main' });

      this.stats = {
          type,
          position, 
          owner,
          image

      }

    }
    
}